npm i
 دستور برای نصب  ماژول

حالا می‌توانی هر تغییر جدید را با مراحل زیر آپلود کنی:

git add .
git commit -m "توضیح تغییرات"
git push


برای دریافت تغییرات از GitHub (مثلاً روی سیستم دوم یا اگر کسی تغییر داد)، از این استفاده کن:

git pull